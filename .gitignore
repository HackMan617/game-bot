__pycache__/
*.pyc
checkpoints/
*.pkl
*.png
!assets/*.png
*.log
.venv/
venv/

# Geode mod build artifacts
geode-mod/build/
*.geode

# Training runs: checkpoints, CSV logs, reports
runs/
*.pt

# Live training run logs (archived NEAT era)
live_log/

# Local tooling / config — never commit (may hold secrets)
.claude/
.mcp.json
UpgradeLog.htm
