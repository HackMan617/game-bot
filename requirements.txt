# Core learning + simulator (Phase 0/1 — no live game needed)
neat-python==0.92
numpy>=1.24
pygame>=2.5
matplotlib>=3.7

# Live-game backend: state comes from the GDBot Bridge Geode mod (see geode-mod/)
# via shared memory, read with the Python stdlib `mmap` — no extra deps needed.
# (GD 2.2 is 64-bit with no public offsets, so we use a Geode mod, not raw pymem.)

# Reinforcement-learning path (Phase 6 — deferred)
# stable-baselines3>=2.2
# torch>=2.2
