# Core: the PPO agent and the environments.
numpy>=1.24
torch>=2.2

# The browser viewer: a websocket push channel plus the stdlib http server.
websockets>=12

# Offline reports over the CSV logs a run writes to runs/<name>/.
matplotlib>=3.7

# The live backend needs no extra packages — state comes from the GDBot Bridge
# Geode mod (see geode-mod/) through named shared memory and two Win32 events,
# read with the stdlib mmap and ctypes. Windows only, by way of the game.
